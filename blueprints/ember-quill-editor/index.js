/*jshint node:true*/
module.exports = {
  description: '',

  /*locals: function(options) {
    // Return custom template variables here.
    return {
      foo: options.entity.options.foo
    };
  },*/


  // bringt nix, da quill über bower keine kompilierte js und css ausgibt.
  // Müsste man also selbst builden, was jetzt zu viel wäre.

  /*afterInstall: function(options) {
    this.ui.writeLine('[ember-quill-editor] add quill to bower!');
    return this.addBowerPackageToProject('quill', '~1.0.4');
  }*/
};
